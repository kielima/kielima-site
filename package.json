{
  "name": "kielima-site",
  "version": "1.0.0",
  "private": true,
  "description": "Site pessoal de Kiê Lima — kielima.com",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "verificar": "node scripts/verificar.mjs",
    "navegador": "node scripts/navegador.mjs",
    "testar": "npm run verificar && npm run navegador"
  },
  "devDependencies": {
    "wrangler": "^4.123.0",
    "playwright": "^1.62.1"
  }
}
